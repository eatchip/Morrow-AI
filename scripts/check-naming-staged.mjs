#!/usr/bin/env node
/**
 * 闸门 3：命名规范检查。
 *
 * 现状：命名规则（文件名大小写、模块前缀、IPC channel 命名等）尚未由 ADR 决定。
 * 本脚本为占位，永远 exit 0 并在 stderr 打印 TODO 提示。
 *
 * 待独立 SDD `naming-conventions` 落地实际规则后，替换本脚本。
 */
console.error('[naming-check] naming rules TBD (see future ADR). Skipping.');
process.exit(0);
